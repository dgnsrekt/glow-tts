// Package engines contains implementations of different TTS engines.
// Currently supports Piper (offline) and Google Cloud TTS (online).
// Each engine implements the TTSEngine interface from the parent package.
package engines